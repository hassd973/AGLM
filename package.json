{
  "name": "aglm-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "serve -s frontend -l $PORT",
    "build": "echo 'Static site - no build step'"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
